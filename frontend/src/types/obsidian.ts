export interface ObsidianSettings {
  webdav_url: string;
  webdav_user: string;
  webdav_configured: boolean;
  vault_path: string;
  browse_paths: string[];
  resolved_path: string;
  auto_save: boolean;
}
